apiVersion: "v1"
kind: "PersistentVolume"
metadata:
  name: "{{ pv_name }}" 
spec:
  capacity:
    storage: "{{ capacity }}Gi" 
  accessModes:
    - "{{ access_mode }}"
  awsElasticBlockStore: 
    fsType: "ext4" 
    volumeID: "{{ volid }}"
